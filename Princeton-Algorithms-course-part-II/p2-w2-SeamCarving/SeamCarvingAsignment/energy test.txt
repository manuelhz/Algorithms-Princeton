Picture picture = new Picture(args[0]);
        SeamCarver seamCarver = new SeamCarver(picture);
        StdOut.println("|------------------------------------------------------------------------------------------------|");
        for (int i = 0; i < seamCarver.energy.length; i ++) {
            for (int j = 0; j < seamCarver.energy[0].length; j++)
                StdOut.print(seamCarver.energy[i][j] + " ");
            StdOut.println();
            StdOut.println("|------------------------------------------------------------------------------------------------|");
        }
        StdOut.println("|------------------------------------------------------------------------------------------------|");
        for (int i = 0; i < seamCarver.height(); i ++) {
            for (int j = 0; j < seamCarver.width(); j++)
                StdOut.print(seamCarver.energy(j, i) + " ");
            StdOut.println();
            StdOut.println("|------------------------------------------------------------------------------------------------|");

        }
